# ==============================================================================
# Transit ETL Worker Environment Configuration
# ==============================================================================
# Target Ingestion API URL (Your MTrain Django Web App):
INGEST_ENDPOINT=https://malaysia.metro-status.com/api/v1/ingest/line/

# Secret Authorization Bearer Token (Must match INGEST_API_KEY in Django settings):
INGEST_API_KEY=mtrain-secret-etl-key-change-in-prod
